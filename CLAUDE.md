# 바이글 (VIGGLE) — 새 대화가 먼저 읽을 것

**쓰는 사람** JUN · PHB. 개발자가 아니다. 화면을 짚어서 시킨다.
**말투** 한국어. 짧고 곧게. 미사여구·군더더기 없이.

## 이게 무엇인가

작업대다. 폰에서 열고, 프로그램을 열고, 화면을 짚어 클로드에게 시킨다.
**맥은 필요 없다.** 전부 깃허브 페이지 + 파이어베이스에서 돈다.

주소: https://vivivic-cloud.github.io/viggle/

## 대화를 시작하면 먼저 할 일

```bash
python3 tools/지시읽기.py          # 밀린 지시부터 확인
```

밀린 지시가 있으면 **그 내용을 대화창에 그대로 옮겨 적고** 시작한다.
사용자는 폰에서 지시를 남기고 결과는 여기서 본다. 무엇을 받아 무엇을
하고 있는지 안 보이면 답답해한다.

일하는 동안 새 지시를 받으려면:

```bash
VG_EVERY=30 python3 -u tools/지시감시.py    # Monitor 로 걸어 둔다
```

일을 마치면 짚으신 그 자리에도 답을 붙인다:

```bash
VG_WHO=에이엠티 python3 tools/답하기.py "box:amt" "고쳤습니다 …"
```

**누구인지 밝힌다.** 프로그램마다 작업자가 따로 있다. `VG_WHO` 를 주거나 글
첫머리에 `[에이엠티 작업자]` 라고 적으면 `클로드·에이엠티` 로 남는다. 안 밝히면
모두 `클로드` 한 이름이 되어 누가 했는지 알 수 없다.

**답 하나가 지시 하나를 맡는다.** 답하기는 밀린 것 중 가장 오래된 지시에 표를
찍는다. 지시 셋에 답 둘을 붙이면 남은 하나가 그대로 밀린 것으로 보인다.
한 번에 여러 건을 답했으면 `--모두` 를 붙인다.

## 프로그램들

전부 `vivivic-cloud` 조직의 깃허브에 있고 깃허브 페이지로 돈다.

| 박스 | 저장소 | 무엇 |
|---|---|---|
| `box:johon` | `vivivic-apps/조혼가구` | 가구 유통사가 발주를 넣는다 |
| `box:amt` | `vivivic-apps/에이엠티` | 제조사가 그 발주를 받아 재단·엣지·보링·NC 로 나눈다 |
| `box:docs` | `vivivic-docs` | 구글드라이브 서류를 판정해 팀이 같이 본다 |
| `box:workorder` | `vivivic-apps/작업지시서` | A4 여러 장 캔버스, 인쇄로 PDF |
| `box:viggle` | `viggle` (이 저장소) | 작업대 자신 |

`vivivic-apps/shared/viggle.js` 는 프로그램 화면에 얹는 **손잡이**다.
`?viggle=1` 일 때만 켜지고, 0.5초 길게 누르면 그 자리를 짚어 지시를 보낸다.
여러 프로그램이 같이 쓰므로 고칠 때 조심한다.

## 지켜야 할 것

- **묻지 말고 배포한다.** 고치면 커밋·푸시하고 깃허브 페이지 반영까지 기다렸다가
  **라이브에서 실제로 확인**한다. "될 것이다" 말고 잰 값으로 보고한다.
- 조혼가구·에이엠티·서류관리는 **실제 업무 데이터**가 걸려 있다.
  파이어베이스의 발주·재단계획을 지시 없이 건드리지 않는다.
- **안 되면 안 된다고 말한다.** 되는 척하지 않는다.
- 화면은 **폰(375px) 기준**. 넓은 화면은 그다음이다.

## 자료는 어디에

파이어베이스 `vivivic-4b7ef` · `artifacts/vivivic-4b7ef/public/data/`

| 칸 | 무엇 |
|---|---|
| `wt_boxes/{id}` | 박스 하나 |
| `wt_chat/{박스id}` | 업무 대화 |
| `wt_dev/{자리}` | 클로드에게 남긴 지시 ← 여기를 본다 |
| `confirmed_orders` | 확정된 발주 (조혼가구 → 에이엠티) |
| `cutting_plans` | 재단 도면 |

익명 로그인으로 들어간다. 규칙은 `request.auth != null` 하나뿐이라
로그인한 사람이면 누구나 읽고 쓴다.

## 다시 밟지 말 것

- 바깥 프로그램을 틀(iframe) 안에 넣으면 **로그인이 막힌다.** 새 창으로 연다.
- 짧은 class 이름은 겹친다. `.top` 하나 때문에 타일 높이가 다 망가진 적이 있다.
- 발주확정목록에 없는 발주는 **다음 단계에 아무 작용도 남기면 안 된다.**

## 프로그램이 새로 생기면 — 세 가지를 반드시 갖춘다

사장님 말씀(09-13): *"작업대안의 모든 프로그램은 그게 기본값이니 …
새로운프로그램 생길때마다 담당 작업자 미리 선정해놓고"*

새 프로그램이 작업대에 들어오면 **묻지 말고 셋을 다 해 둔다.**

1. **박스** — `wt_boxes` 에 `kind: program` 하나와 그 아래 네 칸
   (`프로그램열기` · `바로가기`(plain) · `코드` · `개발자료`).
   **이름은 사장님이 부르신 그대로.** 한글 이름을 지어 붙이지 않는다 —
   `판재도면기` 로 바꿔 놓았다가 "내가 잘 모르잖아" 하고 꾸중 들었다.
2. **짚어서 지시하는 손잡이** — 프로그램 화면에 이 한 줄을 넣는다.
   `<script src="https://vivivic-cloud.github.io/vivivic-apps/shared/viggle.js"></script>`
   (같은 저장소 안이면 `../shared/viggle.js`, 다른 저장소면 위 절대주소.)
   `?viggle=1` 이 있을 때만 켜지므로 그냥 열면 달라지는 것이 없다.
   작업대가 열 때 `box=<프로그램 박스 id>` 를 붙여 주므로 지시는 그 자리로 쌓인다.
3. **전담 작업자** — `create_session` 으로 하나. 한 프로그램에 하나다.
   `tags: ["config:auto-create-pr:off", "lane:box:<박스id>", "작업자"]`,
   환경은 `env_019ykFbFRXU2HmffPCuqddpG` (여기에 `/home/user/viggle` 이 딸려 와
   `tools/답하기.py` 를 쓸 수 있다). 만들고 나면 **매시 쓸기 명단에 넣는다.**

**답을 올렸다고 일이 끝난 것이 아니다.** `남은것.py` 는 답만 센다. 작업자가
여러 건 중 일부만 하고 답하면 나머지가 0건으로 숨는다 — 실제로 세 건이
사라질 뻔했다. 그래서 쓸기는 작업자가 **멈춰 있는지**도 함께 본다.

## 한 자리를 짚으셔도 집 전체 말씀일 수 있다 — 퍼뜨려라

사장님 말씀(09-13): *"메인화면등 모든 박스가 있는곳에는 박스추가 버튼을 만들어
달라고 했는데 왜 안보입니까 이부분은 조혼가구뿐 아니라 다른 작업대의 모든 것에
적용해 주세요"*

**이건 내(관리자) 잘못이었다.** 사장님은 같은 것을 세 번 말씀하셨다 —
09-11 작업대에서, 09-12·09-13 서류관리에서. 나는 그때마다 **짚으신 그 자리의
작업자에게만** 보냈다. 그래서 조혼가구·에이엠티에는 한 번도 간 적이 없고,
사장님은 "왜 안 보이냐"고 물으셔야 했다.

**지시를 받으면 먼저 이것을 가른다.**
- **그 프로그램만의 일인가** — 그 작업자에게만.
- **집 전체에 걸리는 일인가** — **모든 작업자에게 한꺼번에.**

집 전체에 걸리는 말씀의 표시 — *"모든"* · *"어디서나"* · *"전부"* ·
*"~등"* · 화면 생김새·조작법·손잡이·박스처럼 **어느 프로그램에나 있는 것**을
가리킬 때. 애매하면 **퍼뜨리는 쪽**을 고른다. 안 해서 혼나는 편이
해서 혼나는 편보다 훨씬 나쁘다.

퍼뜨릴 때는 **본을 하나 정해 주고 베끼게 한다.** 저마다 다른 모양이 나오면
사장님이 같은 집으로 안 보신다. 이미 한 프로그램이 만들어 두었으면 그것이 본이고,
그 작업자에게 **"다른 작업자가 읽고 따라 할 수 있게 만든 법을 적어라"** 고 시킨다.
박스가 없는 프로그램에는 **억지로 만들게 하지 않는다** — "없습니다" 라고 답하게 한다.

## 집 전체 규칙이 된 것들 (새 프로그램도 이대로)

- **짚어서 지시하는 손잡이** — 모든 프로그램. (위 세 가지 참고)
- **박스가 있는 화면에는 「박스추가」** — 본은 작업대 `index.html` 의 `새 박스`
  (FAB `id="fab"` · `newboxbtn` · 길게 눌러 `여기에 새 박스`), 그리고 서류관리 v0.29.0~.
  **일이 돌아가는 박스는 지워지거나 옮겨지면 안 된다.** 사장님이 만드신 박스만 그렇게 된다.
  만드신 박스는 **파이어스토어에 담아 기기를 따라오게** 한다 — 사장님은 아이폰으로도
  넓은 화면으로도 일하신다. 다만 `wt_boxes`·`wt_dev`·`confirmed_orders`·`cutting_plans`
  에는 쓰지 않는다. 프로그램마다 제 칸을 만든다.
- **화면은 폰(375px) 기준**, 마우스·넓은 화면(1280px)도 함께 본다.
